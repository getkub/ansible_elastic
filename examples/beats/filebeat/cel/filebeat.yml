filebeat.inputs:
- type: cel
  interval: 10m
  resource:
    url: https://api.sunrise-sunset.org/json?lat=36.7201600&lng=-4.4203400
  program: |
    string('test').as(e,{
            "t": now,
    }).as(res,
        {
            "events": res,
            "finalValue" : "only_this_needs_be_displayed",
        }
    ).as(res2,
        {
            "events": res2.finalValue,
        }
    )


logging.level: debug
logging.metrics.enabled: false
logging.to_files: false

output.console:
  pretty: true
