---

- name: "Beats: Validate Beats suitablility for remote clients"
  import_tasks: beats_validate_clients.yml
  tags: [beats_validate_clients]

- name: "Beats: Copoy Beats installables into remote clients"
  import_tasks: beats_clients_copy.yml
  tags: [beats_clients_copy]

- name: "Beats: Install Beats into remote clients"
  import_tasks: beats_clients_install.yml
  tags: [beats_clients_install]
